description: |
  Smart document generator.
prompt: |
  Create a single document that aheaders to the schema. 
  Do not include ``` in the output. 
schema: |
  {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "items": {
      "type": "object",
      "properties": {
        "page_content": {
          "type": "string"
        },
        "metadata": {
          "type": "object",
          "properties": {},
          "additionalProperties": true
        }
      },
      "required": ["page_content", "metadata"]
    }
  }
example: |
  {
    "page_content": "Once upon a time in a quaint little village nestled between rolling hills and lush forests, there",
    "metadata": {}
  }
